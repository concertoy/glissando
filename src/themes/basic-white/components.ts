/**
 * Basic White uses the same config-driven component factory as claude-doc.
 * All colors, fonts, and sizes are read from the ThemeConfig at creation time.
 */

export { createComponents } from "../claude-doc/components.js";
